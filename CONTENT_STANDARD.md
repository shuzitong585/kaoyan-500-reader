# 《考研500高频词沉浸式阅读器》数据生产标准

本文件用于约束 EP05—EP22 及后续词汇数据生产。EP01 是正式产品标准，不是宽松参考。制作任何新章节前，必须先读取本文件，并检查 `data/ep01.js` 的实际数据结构与内容密度。

## 一、章节生产原则

1. 每个新章节必须复制 EP01 的章节结构和词卡结构。
2. 后续章节只替换章节编号、标题、正文和单词数据，不重新设计词卡结构。
3. 不得修改已经验收的章节正文、目标词、页面结构或交互逻辑。
4. 正文目标词必须与词卡数据一一对应，不得遗漏、新增或擅自改变词形。
5. 不允许为了加快生产而删减栏目、减少内容或使用无实际学习价值的占位内容。

## 二、单词必备字段

每个目标词必须包含以下字段，字段名与 EP01 完全一致：

```text
phonetic
pos
context
core
meanings
wordFormation
mnemonic
relatedWords
collocations
storyHook
```

字段是否展示服从实际学习价值，不要求为了形式完整而全部强制非空：

- `wordFormation` 与 `mnemonic` 按单词特点二选一。
- `relatedWords` 没有真实、高价值内容时可以为空数组，并隐藏整个模块。
- 其他展示内容必须准确、有效，不得使用“暂无”“整体记忆”“后续补充”等占位内容。
- 禁止为了填满字段制造低质量、牵强或错误内容。

## 三、字段内容标准

### phonetic

- 提供准确、规范的音标。
- 同一项目内保持音标体系和书写格式一致。

### pos

- 使用规范词性缩写，如 `n.`、`v.`、`adj.`、`adv.`。
- 单词具有多个高频词性时可以并列，但不收录低价值词性。

### context

- 只解释目标词在当前章节剧情中的意思。
- 表达准确、简短，便于点击后快速理解。
- 不扩展与本文无关的词义。

### core

- 只保留考研阅读中最值得掌握的核心含义。
- 简洁、准确，方便扫读和记忆。
- 不堆积生僻或低价值词典义。

### meanings

- 保留 2—4 个考研高频、常见义项。
- 每条注明词性。
- 较少见但仍有学习价值的义项应标注“较少见”。
- 禁止为了丰富内容加入生僻义。

### memoryMethod｜记忆方法分级

每个单词必须按照以下优先级选择一种记忆方法。`memoryMethod` 是生产判断标准，不新增为数据字段，以保持现有词卡结构兼容。

#### 词源与构词判断优先级

按以下顺序逐层判断，前一层成立时不再降级使用后一层：

1. **真实复合词**：两个或多个可独立使用的单词构成新词，组合关系能够准确解释整体词义。例如 `drawback = draw + back`。
2. **真实词根词缀**：构词层级明确，词根、前缀或后缀的功能真实可靠。例如 `skeptical = skeptic + -al`。
3. **历史词源关系**：现代词形不适合直接拆分，但有经过确认、能够帮助理解词义发展的历史来源。
4. **scene / mnemonic**：以上三层均不存在、不可靠或没有实际学习价值时，使用场景记忆。

禁止仅根据现代拼写或字母位置切割单词。组成片段即使恰好与某个现代单词相同，也必须确认其属于真实构词层级；无法确认时不得使用 `wordFormation`。

#### wordFormation 强制准入条件

只有同时满足以下五项条件，才允许使用 `wordFormation`：

1. 拆分结构真实存在。
2. 拆分关系符合英语构词规律及该词的真实构词层级。
3. 拆分内容能够帮助学生理解当前核心词义。
4. 解释过程不依赖现代中文谐音、字面附会或人为联想。
5. 不为了增强记忆效果创造、改写或夸大不存在的词源关系。

判断结果：

```text
五项全部满足 → wordFormation
任一项不满足 → mnemonic
```

#### 1. root｜词源词根

- 第一优先级。
- 仅使用经过确认、真实可靠且有助于理解词义的词源或词根关系。
- 需要说明词根如何发展出当前核心词义，使学习者理解“为什么这个词是这个意思”。
- 使用 `wordFormation` 字段展示。
- 推荐使用短行和箭头，不写成长篇历史词源说明。

#### 2. affix｜词缀拆解

- 第二优先级。
- 适用于词干、前缀或后缀关系清晰的单词。
- 拆解必须符合真实构词关系，能够说明词性或词义的形成过程。
- 使用 `wordFormation` 字段展示。
- 不得仅因词形相似，就把字母组合解释成词根或词缀。

#### 明显复合词检查

- 如果一个词由两个可独立使用的英语单词构成，并且两部分组合后能够自然、准确地解释整体词义，优先使用 `wordFormation`。
- 复合词拆解的优先级高于普通场景记忆，不得用泛化的 `mnemonic` 代替清晰的复合构词关系。
- 必须确认两部分确实是该词的构词成分，不能仅凭表面字母相同进行拆分。
- 展示时先说明两个组成词的核心含义，再用箭头连接到复合词义。

推荐格式：

```text
draw = 拉
back = 后面
↓
拉回来的阻碍
↓
drawback = 缺点
```

同类示例：

```text
feed = 提供
back = 回来
↓
feedback = 反馈

fore = 前面
cast = 投
↓
提前投向未来
↓
forecast = 预测
```

#### 3. scene｜场景记忆

- 第三优先级，仅在没有可靠、实用的词源词根或词缀拆解时使用。
- 使用 `mnemonic` 字段展示，并将 `wordFormation` 设为空。
- 必须提供具体、可复现的动作、画面、对比或场景，让场景与核心词义建立清晰联系。
- 普通释义、同义改写或再次解释中文含义，不属于有效的场景记忆。
- 内容应简洁，通常 2—3 行即可理解。

### wordFormation 与 mnemonic 的存储规则

- `root` 和 `affix` 写入 `wordFormation`，`mnemonic` 设为空。
- `scene` 写入 `mnemonic`，`wordFormation` 设为空。
- `wordFormation` 与 `mnemonic` 必须严格二选一，不得同时填写或同时为空。
- 禁止编造词根、使用错误词源、错误拆词或为了填字段强行拆分。
- 禁止使用“把它作为一个整体记忆”等无效占位内容。
- 最终目标不是形式上的拆解，而是帮助学习者理解词形、记忆路径与核心词义之间的关系。

### relatedWords

- 只提供真实、有价值的词族词或高价值关联词。
- 优先保留能帮助考研阅读和构词理解的派生词。
- 禁止添加关系薄弱、低频或仅为填充模块的词。
- 如果没有合适内容，可以设为空数组，并隐藏整个模块。

### collocations

- 只提供考研英语阅读或实际语言使用中的高频常见搭配。
- 每条搭配附准确中文意思。
- 原则上保留 2—3 个，最多 3 个。
- 不收录生僻、牵强、低价值或仅用于凑数量的普通短语。
- 数量服从质量；宁可少而准确，也不能为了填满3条降低标准。

### storyHook

- 必须与当前章节剧情直接相关。
- 控制在 1—2 个短句。
- 必须形成有效的剧情记忆钩子，不写成剧情摘要或人物心理分析。
- 不直接复制正文原句。

## 四、质量优先原则

数据取舍以学习价值为最高标准：

```text
本文语境理解
＞
考研常见义与高频搭配
＞
可靠的构词或记忆辅助
＞
扩展内容与字段数量
```

- 质量优先于字段完整。
- EP05—EP22 的目标是保持 EP01 的内容质量和真实产品体验，而不是满足字段数量。
- 字段为空不一定是不合格；错误、牵强、低价值的填充内容才是不合格。

## 五、制作前检查

制作 EP05—EP22 任一章节前，必须先完成：

1. 读取 `CONTENT_STANDARD.md`。
2. 读取 `CONTENT_RULES.md`。
3. 检查 `data/ep01.js` 的章节封装、正文标记、字段名称和内容密度。
4. 提取新章节全部目标词并与既有章节去重核对。
5. 确认正文目标词数量、唯一词卡数量和点击映射一致。

## 六、完成后验收

每章完成后必须检查：

1. 正文目标词与词卡一一对应。
2. 每个词都沿用全部十个标准字段名，允许按本规则隐藏可选模块。
3. `wordFormation` 与 `mnemonic` 至少一个包含可靠、有效的学习内容。
4. `relatedWords` 为空时必须确实没有高价值词族；不得为了非空而强行填充。
5. `meanings` 保留 2—4 个高价值义项。
6. `collocations` 不超过 3 个且均有中文释义。
7. `wordFormation` 不含伪词根或错误词源。
8. 非空的 `relatedWords` 均为真实、高价值关联词。
9. `storyHook` 与本章剧情相关且不复制正文。
10. 页面点击词汇后能按 EP01 的固定顺序显示完整词卡。

只有全部检查通过，章节数据才可标记为 `available`。

## 七、wordFormation 最终三问审核

每个非空的 `wordFormation` 在交付前必须逐词回答：

1. 拆分是否真实？
2. 拆分是否符合英语构词规律及该词的真实构词层级？
3. 拆分是否确实帮助学生理解当前核心词义？

判定规则：

```text
三个问题全部为“是” → 保留 wordFormation
任意一个问题为“否” → 删除 wordFormation，改用有效 mnemonic
```

不得因为拆分看起来容易记、栏目需要内容或希望提高 `wordFormation` 数量而降低审核标准。
